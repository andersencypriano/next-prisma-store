/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'fakestoreapi.com',
      'dummyjson.com',
      'cdn.dummyjson.com',
    ],
  }
};

export default nextConfig;
